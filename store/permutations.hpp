//
//  permutations.hpp
//  algorithms
//
//  Created by Igor on 31.05.2025.
//

#ifndef permutations_hpp
#define permutations_hpp

#include <stdio.h>
#include <string>
#include <vector>

#endif /* permutations_hpp */
std::vector<std::string> permutations(std::string s);
std::vector < int> josephus(std::vector < int > items, int k);

/* Basically you have to assume that n people are put into a
 circle and that they are eliminated in steps of k elements */
int josephusSurvivor(int n, int k);
